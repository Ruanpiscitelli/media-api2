# API
DEBUG=false
HOST=0.0.0.0
PORT=8000
WORKERS=4
LOG_LEVEL=info

# Segurança
SECRET_KEY=${SECRET_KEY}  # Definir via variável de ambiente
ALGORITHM=HS512
ACCESS_TOKEN_EXPIRE_MINUTES=15

# CORS
CORS_ORIGINS=${CORS_ORIGINS}  # Definir via variável de ambiente

# Redis
REDIS_HOST=${REDIS_HOST}
REDIS_PORT=${REDIS_PORT}
REDIS_DB=0
REDIS_PASSWORD=${REDIS_PASSWORD}

# GPU
CUDA_VISIBLE_DEVICES=0,1,2,3
MAX_BATCH_SIZE=8
VRAM_BUFFER_MB=2048

# Rate Limiting
RATE_LIMIT_PER_MINUTE=30
RATE_LIMIT_PER_HOUR=500

# Monitoramento
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000

# Modelos
SDXL_MODEL_PATH=/opt/models/sdxl/base
FISH_SPEECH_MODEL_PATH=/opt/models/fish_speech/base
FAST_HUAYUAN_MODEL_PATH=/opt/models/fast_huayuan/base

# Cache
CACHE_TTL=1800
MAX_CACHE_SIZE_MB=51200

# Celery
CELERY_BROKER_URL=${CELERY_BROKER_URL}
CELERY_RESULT_BACKEND=${CELERY_RESULT_BACKEND} 